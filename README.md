# Prometheus Modular Stack

**Библиотека модульных конфигураций** для развертывания системы мониторинга (Prometheus, Grafana, Alertmanager).

> **Суть проекта:** Это "Конструктор" и учебное пособие. Каждая строка конфигурации (`yaml`, `py`, `conf`) содержит подробный комментарий на русском языке: *зачем* нужен этот параметр, *как* он работает и *почему* это важно.
> Проект идеально подходит для изучения принципов работы Prometheus или как заготовка (Boilerplate) для сборки собственной инфраструктуры.

---

## 📂 Структура и Назначение модулей

Проект состоит из независимых блоков (стеков). Каждый блок изолирован в своей папке и имеет собственный `docker-compose.yml`.

```text
.
├── docker-compose.yml          # MASTER-файл. Запускает ВСЕ модули сразу (для локальных тестов).
│
├── prometheus-stack/           # [ЯДРО] Сердце системы.
│   ├── Prometheus              # Сбор и хранение метрик.
│   ├── Alertmanager            # Управление уведомлениями (Telegram, Email).
│   ├── Grafana                 # Визуализация (Дашборды).
│   └── Node Exporter           # Мониторинг "здоровья" самого сервера мониторинга.
│
├── app-stack/                  # [ПРИЛОЖЕНИЕ] Пример Python (FastAPI) сервиса.
│   # Демонстрирует, как внедрить метрики (Prometheus Client) прямо в код приложения.
│
├── nginx-stack/                # [WEB] Пример веб-сервера (Раздача статики).
│   # Демонстрирует паттерн "Sidecar": связку Nginx + Nginx Exporter для сбора статистики.
│
├── postgres-stack/             # [DATABASE] Пример базы данных PostgreSQL.
│   # Демонстрирует мониторинг БД через отдельный контейнер-экспортер.
│
├── node-exporter/              # [LINUX AGENT] Агент для установки на удаленные Linux-серверы.
│
└── node-win-exporter/          # [WINDOWS AGENT] Инструкции и конфиги для Windows-серверов.
```

---

## 🚀 Как запустить

### Вариант 1: Локальный запуск (Всё-в-одном)
Используется корневой файл, объединяющий все стеки через `include`.
*Требуется Docker Compose v2.20+.*

```bash
docker compose up -d
```

### 🔌 Карта портов (Cheatsheet)

#### 1. Ядро Мониторинга (Core Stack)
| Сервис | Порт (UI) | Описание |
| :--- | :--- | :--- |
| **Grafana** | `3000` | Визуализация, вход `admin`/`admin` |
| **Prometheus** | `9090` | Веб-интерфейс сервера мониторинга |
| **Alertmanager** | `9093` | Панель управления алертами |

#### 2. Инфраструктура (Host Agents)
| Агент | Порт метрик | Описание |
| :--- | :--- | :--- |
| **Node Exporter** | `9100` | Метрики Linux (CPU, Mem, Disk) |
| **Win Exporter** | `9182` | Метрики Windows (Remote) |

#### 3. Сервисы (Services by Stack)

**`app-stack`**
| Сервис | Порт | Роль | Описание |
| :--- | :--- | :--- | :--- |
| **Python App** | `8000` | APP + METRICS | API и метрики на одном порту |

**`nginx-stack`**
| Сервис | Порт | Роль | Описание |
| :--- | :--- | :--- | :--- |
| **Nginx** | `8080` | WEB | Раздача статики |
| **Exporter** | `9113` | METRICS | Сайдкар для мониторинга |

**`postgres-stack`**
| Сервис | Порт | Роль | Описание |
| :--- | :--- | :--- | :--- |
| **Postgres** | `5432` | DATABASE | База данных |
| **Exporter** | `9187` | METRICS | Сайдкар для мониторинга |

---

## ⚙️ Важные технические нюансы

### 1. Права доступа (Linux Permissions)
Для наглядности данные сохраняются в папки `./data` (Bind Mounts), чтобы вы могли их видеть.
В Linux это часто вызывает ошибку `Permission Denied`, так как процессы внутри контейнера работают под специфичными пользователями.

**Решение (выполнить на хосте):**
```bash
# Для Prometheus и Alertmanager (UID 65534 - 'nobody')
sudo chown -R 65534:65534 prometheus-stack/data
sudo chown -R 65534:65534 prometheus-stack/alertmanager_data

# Для Grafana (UID 472 - 'grafana')
sudo chown -R 472:472 prometheus-stack/grafana_data

# Для Postgres (UID 999 - 'postgres')
sudo chown -R 999:999 postgres-stack/data
```
*На Windows (Docker Desktop) это происходит автоматически, действий не требуется.*

### 2. Сеть (Network Mode)
*   **`network_mode: "host"`**: Использован для `node-exporter` и стека мониторинга. Контейнеры используют сетевой интерфейс хоста напрямую. Это упрощает доступ к "железу" и взаимодействие компонентов на одном сервере.
*   **`bridge`**: Использован для приложений (`app`, `postgres`, `nginx`), чтобы изолировать их порты.

---

## 🛠 Настройка под себя

Все настройки находятся в папках `config/` внутри каждого модуля.
*   **Кого мониторить:** `prometheus-stack/config/prometheus.yml`
*   **Правила тревог:** `prometheus-stack/config/alert_rules.yml`
*   **Куда слать алерты:** `prometheus-stack/config/alertmanager.yml`
